`timescale 1ns / 1ps

module input_module(
    input  wire clk,
    input  wire sin,
    output reg  [7:0] sout
);

    initial sout = 8'd0;

    always @(posedge clk) begin
        sout <= {7'b0000000, sin};
    end

endmodule
